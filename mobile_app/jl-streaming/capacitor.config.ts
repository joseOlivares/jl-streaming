import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'jlstreaming.iconnsv.com',
  appName: 'jl-streaming',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
